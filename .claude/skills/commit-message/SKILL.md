---
name: commit-message
description: Gitにステージングされている変更内容からコミットメッセージを考え、許可を求めずそのままコミットする
disable-model-invocation: false
---

Gitにステージングされている変更（`git add` 済みのもの）を分析し、適切なコミットメッセージを日本語で考え、**許可を求めずにそのままコミットまで実行する**。

## 手順

1. ステージングされている変更を確認する:
   ```
   git diff --cached
   ```
   - 何もステージングされていない場合は、その旨をユーザーに伝えて終了する（`git add` を促す）。

2. 変更内容を分析する:
   - 変更されたファイルと差分の内容を読み取る。
   - 何を・なぜ変更したのかを把握する（機能追加・バグ修正・リファクタリング・ドキュメント更新など）。

3. コミットメッセージを Conventional Commits 形式で組み立てる:
   - **1行目（subject）**: `<type>: <要約>` の形式。日本語で簡潔に（50文字程度まで）。
     - type の例: `feat`（機能追加）, `fix`（バグ修正）, `docs`（ドキュメント）, `refactor`（リファクタリング）, `style`（整形）, `test`（テスト）, `chore`（雑務・設定）
   - 必要に応じて空行を挟み、**本文（body）** で変更の理由や詳細を箇条書きで説明する。

4. 組み立てたコミットメッセージで、**許可を求めずにそのままコミットを実行する**:
   ```
   git commit -m "<subject>" -m "<body>"
   ```
   - 実行したコミットメッセージとコミット結果（ハッシュ等）をユーザーに報告する。

## 注意

- コミットメッセージは必ず日本語で作成する。
- **確認は取らず、メッセージを決めたらそのままコミットする。**
- 変更が複数の異なる目的を含む場合でも、まとめて1コミットする（分割が必要そうなら報告時に指摘する）。

---

## 使い方

Claude Code上で以下を入力するだけで実行されます。
/commit-message
